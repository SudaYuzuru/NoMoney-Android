<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test">

    <application
        android:name=".UniteApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".nomoneyWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/nomoney_widget_info" />
        </receiver>

        <activity android:name=".NotificationActivity" />
        <activity android:name=".ClockActivity" />
        <activity
            android:name=".AlarmActivity"
            android:label="@string/add_alarm" />
        <activity android:name=".SmallBoxActivity" />
        <activity android:name=".MonthChartActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".RecordActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name="animation.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>